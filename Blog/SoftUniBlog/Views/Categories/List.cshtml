@model List<SoftUniBlog.Migrations.Models.Categories>

@{
    ViewBag.Title = "List";
}

<div class="container">
    <div class="well">
        <h2>All Categories - @Html.ActionLink("Create", "Create", "Categories", null, new { @class = "btn btn-warning" })</h2>
        <div class="row">
            <table class="table table-striped table-hover col-sm-12">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td>@category.Id</td>
                            <td>@category.Name</td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", "Categories", new { @id = category.Id }, null)
                                @Html.ActionLink("Delete", "Delete", "Categories", new { @id = category.Id }, null)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
